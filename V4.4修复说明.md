# V4.4 同步修复

本版修复两个实际的同步阻塞问题：

1. `initialCloudSync()` 与 `pushCloud()` 共用 `cloudBusy`，导致首次无云端记录时写入被自己阻止。
2. `onAuthStateChange` 中直接执行 async Supabase API 可能触发 supabase-js deadlock。
   V4.4 改为 auth callback 只更新 session，数据库请求延迟到下一个 event-loop tick 执行。

新增：
- 「云端记录：未检查 / 不存在 / 已存在」
- 同步失败时直接显示 Supabase 错误信息

## 更新 GitHub

覆盖：
- index.html
- 404.html
- app.js
- styles.css
- sw.js

Commit message：

Fix Supabase sync deadlocks V4.4

部署后建议：
1. Mac 和 iPhone 都重新打开网页 / PWA。
2. 进入「同步 / 安装与数据」。
3. 确认登录状态。
4. 在有数据的 Mac 点击一次「立即同步」。
5. 看到「云端记录：已存在」「当前状态：已同步」。
6. iPhone 点「从云端刷新」。
